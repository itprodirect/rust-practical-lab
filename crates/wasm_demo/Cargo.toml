[package]
name = "wasm_demo"
version = "0.1.0"
edition = "2021"
description = "WASM interop demo with wasm-bindgen"
license = "MIT"
repository = "https://github.com/itprodirect/rust-practical-lab"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
