# Session Log - 2026-03-27

## Objective

Rename the repository from `rust-hello-world` to `rust-practical-lab` and install a minimal memory stack for easier human and agent handoff.

## Starting State

- Branch: `main`
- Worktree note: untracked `.claude/` directory present before changes
- Repo identity already described the project as a practical Rust workspace, despite the old slug

## What The Repo Currently Does

This workspace teaches Rust through runnable labs and reusable modules. It includes a hello CLI, error-handling and concurrency demos, a log parser and benchmark, FFI and WASM interop examples, and a `power_blocks` crate of copyable patterns.

## Actions Taken

- Renamed top-level identity references from `rust-hello-world` to `rust-practical-lab`
- Added `AGENTS.md` with read order and tight working rules
- Added repo-memory index entries in `docs/README.md`
- Added `docs/repo-brief.md` for stable repo context
- Added `docs/heartbeat.md` for short operational state
- Added `docs/decisions.md` to record the rename decision
- Updated repository URL metadata in crate manifests
- Renamed the GitHub repository and updated local `origin` to `https://github.com/itprodirect/rust-practical-lab.git`

## Validation

- `cargo fmt --all -- --check`: passed
- `cargo test --workspace`: passed
- `cargo run -p hello_cli -- --name world`: passed (`Hello, world!`)
- `git remote -v`: points to `https://github.com/itprodirect/rust-practical-lab.git`

## Follow-Up

Keep future session logs dated and focused on concrete repo changes, validation, and unresolved issues.
